import { TelefoneFormatadoPipe } from './telefone-formatado.pipe';

describe('TelefoneFormatadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefoneFormatadoPipe();
    expect(pipe).toBeTruthy();
  });
});
